---
# ✅ Radarr Deployment & Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: radarr
  namespace: plex
  labels:
    app: radarr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: radarr
  template:
    metadata:
      labels:
        app: radarr
    spec:
      nodeSelector:
        kubernetes.io/hostname: casaos
      tolerations:
        - key: "node.kubernetes.io/disk-pressure"
          operator: "Exists"
          effect: "NoSchedule"
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      containers:
      - name: radarr
        image: lscr.io/linuxserver/radarr:latest
        env:
        - name: PUID
          value: "1000"
        - name: PGID
          value: "1000"
        - name: TZ
          value: "America/Toronto"
        volumeMounts:
        - name: config
          mountPath: /config
        - name: media
          mountPath: /data
        ports:
        - containerPort: 7878
      volumes:
      - name: config
        hostPath:
          path: /mnt/media/radarr
          type: Directory
      - name: media
        hostPath:
          path: /mnt/media
          type: Directory
---
apiVersion: v1
kind: Service
metadata:
  name: radarr
  namespace: plex
  labels:
    app: radarr
spec:
  type: ClusterIP
  selector:
    app: radarr
  ports:
  - name: http
    port: 7878
    targetPort: 7878
---
# ✅ Sonarr Deployment & Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sonarr
  namespace: plex
  labels:
    app: sonarr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sonarr
  template:
    metadata:
      labels:
        app: sonarr
    spec:
      nodeSelector:
        kubernetes.io/hostname: casaos
      tolerations:
        - key: "node.kubernetes.io/disk-pressure"
          operator: "Exists"
          effect: "NoSchedule"
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      containers:
      - name: sonarr
        image: lscr.io/linuxserver/sonarr:latest
        env:
        - name: PUID
          value: "1000"
        - name: PGID
          value: "1000"
        - name: TZ
          value: "America/Toronto"
        volumeMounts:
        - name: config
          mountPath: /config
        - name: media
          mountPath: /data
        ports:
        - containerPort: 8989
      volumes:
      - name: config
        hostPath:
          path: /mnt/media/sonarr
          type: Directory
      - name: media
        hostPath:
          path: /mnt/media
          type: Directory
---
apiVersion: v1
kind: Service
metadata:
  name: sonarr
  namespace: plex
  labels:
    app: sonarr
spec:
  type: ClusterIP
  selector:
    app: sonarr
  ports:
  - name: http
    port: 8989
    targetPort: 8989
---
# ✅ Bazarr Deployment & Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bazarr
  namespace: plex
  labels:
    app: bazarr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bazarr
  template:
    metadata:
      labels:
        app: bazarr
    spec:
      nodeSelector:
        kubernetes.io/hostname: casaos
      tolerations:
        - key: "node.kubernetes.io/disk-pressure"
          operator: "Exists"
          effect: "NoSchedule"
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      containers:
      - name: bazarr
        image: lscr.io/linuxserver/bazarr:latest
        imagePullPolicy: Always
        env:
        - name: PUID
          value: "1000"
        - name: PGID
          value: "1000"
        - name: TZ
          value: "America/Toronto"
        ports:
        - name: http
          containerPort: 6767
        volumeMounts:
        - name: config
          mountPath: /config
        - name: media
          mountPath: /data
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 90
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 5
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 90
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
      volumes:
      - name: config
        hostPath:
          path: /mnt/media/bazarr
          type: DirectoryOrCreate
      - name: media
        hostPath:
          path: /mnt/media
          type: Directory
---
apiVersion: v1
kind: Service
metadata:
  name: bazarr
  namespace: plex
  labels:
    app: bazarr
spec:
  type: ClusterIP
  selector:
    app: bazarr
  ports:
  - name: http
    port: 6767
    targetPort: http
    protocol: TCP
---
# ✅ Prowlarr Deployment & Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prowlarr
  namespace: plex
  labels:
    app: prowlarr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prowlarr
  template:
    metadata:
      labels:
        app: prowlarr
    spec:
      nodeSelector:
        kubernetes.io/hostname: casaos
      tolerations:
        - key: "node.kubernetes.io/disk-pressure"
          operator: "Exists"
          effect: "NoSchedule"
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      containers:
      - name: prowlarr
        image: lscr.io/linuxserver/prowlarr:latest
        env:
        - name: PUID
          value: "1000"
        - name: PGID
          value: "1000"
        - name: TZ
          value: "America/Toronto"
        volumeMounts:
        - name: config
          mountPath: /config
        - name: media
          mountPath: /data
        ports:
        - containerPort: 9696
      volumes:
      - name: config
        hostPath:
          path: /mnt/media/prowlarr
          type: Directory
      - name: media
        hostPath:
          path: /mnt/media
          type: Directory
---
apiVersion: v1
kind: Service
metadata:
  name: prowlarr
  namespace: plex
  labels:
    app: prowlarr
spec:
  type: ClusterIP
  selector:
    app: prowlarr
  ports:
  - name: http
    port: 9696
    targetPort: 9696
---
# ✅ Homarr Deployment & Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: homarr
  namespace: plex
  labels:
    app: homarr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: homarr
  template:
    metadata:
      labels:
        app: homarr
    spec:
      nodeSelector:
        kubernetes.io/hostname: casaos
      tolerations:
        - key: "node.kubernetes.io/disk-pressure"
          operator: "Exists"
          effect: "NoSchedule"
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      containers:
      - name: homarr
        image: ghcr.io/ajnart/homarr:latest
        env:
        - name: PUID
          value: "1000"
        - name: PGID
          value: "1000"
        - name: TZ
          value: "America/Toronto"
        volumeMounts:
        - name: config
          mountPath: /app/data/configs
        - name: icons
          mountPath: /app/public/icons
        - name: data
          mountPath: /data
        ports:
        - containerPort: 7575
      volumes:
      - name: config
        hostPath:
          path: /mnt/media/homarr/configs
          type: DirectoryOrCreate
      - name: icons
        hostPath:
          path: /mnt/media/homarr/icons
          type: DirectoryOrCreate
      - name: data
        hostPath:
          path: /mnt/media/homarr/data
          type: DirectoryOrCreate
---
apiVersion: v1
kind: Service
metadata:
  name: homarr
  namespace: plex
  labels:
    app: homarr
spec:
  type: ClusterIP
  selector:
    app: homarr
  ports:
  - name: http
    port: 7575
    targetPort: 7575

